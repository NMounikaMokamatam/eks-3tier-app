# ⚠️  DO NOT commit real values
# Generate with:
# kubectl create secret generic db-secret \
#   --from-literal=dbname=appdb \
#   --from-literal=username=postgres \
#   --from-literal=password=STRONG_PASSWORD \
#   -n app --dry-run=client -o yaml | kubeseal > k8s/db/sealed-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: app
type: Opaque
stringData:
  dbname:   appdb
  username: postgres
  password: REPLACE_WITH_STRONG_PASSWORD
